let person = {
    name: "Vamsi",
    age: 25
};


//  person is an object containing key-value pairs.


console.log(typeof person);

console.log(person.age);  // 1st way (dot notation)
console.log(person.name);

console.log(person["age"]); // 2nd way (bracket notation)
console.log(person["name"]);